The flask-web service runs on portal-auth,
it reads data form mongoDB to which credentials are stored on a separate file.

To set my enviroment on  portal-auth  execute:

module load python/2.7.3
source ~/env273/bin/activate
pip freeze

. ~/.ssh/star-mongoDb.pass 


To launch service exec:

./appStarProdMon.fcgi 

  
 /star-hpc (for your production tool) port for  .fcgi script  8875
https://portal-auth.nersc.gov/star-hpc/


/star-hpc-dev (for your development tool).  8874
https://portal-auth.nersc.gov/star-hpc-dev/
